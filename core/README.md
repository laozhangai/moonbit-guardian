# MoonBit Guardian Core

The reusable MoonBit package implements text, exit-code, and RFC 6901 JSON Pointer assertions. The native `cmd/server` package adds project inspection, toolchain commands, MCP stdio, and delivery-contract execution.

This is a community project and is not an official MoonBit component.
